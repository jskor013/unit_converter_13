# 03. 테스트플랜및RED체크리스트 작업 보고서

생성일: 2026-05-21  
워크스페이스: `E:\dev\unit_converter_13`  
대상 브랜치: `RED`

## 1. 작업 목적

README와 PRD의 단위 변환 계약을 기준으로 `meter -> feet` 변환을 테스트 플랜 샘플로 선정하고, RED 단계에서 작성할 Catch2 테스트 범위와 README 체크리스트를 문서화했다.

이번 작업은 구현 코드 작성이 아니라 테스트 설계와 RED 단계 작업 항목 정리를 목적으로 수행되었다.

## 2. 변경 파일

문서:

- `docs/test_plan.md`
  - `meter -> feet` 변환을 테스트 대상 기능으로 정의했다.
  - 입력 예시 `meter:2.5`, 기대 출력 `2.5 meter = 8.202100 feet`, 기준 비율 `1 meter = 3.28084 feet`를 기록했다.
  - Catch2 기반 테스트 범위를 P0/P1/P2 우선순위로 나누었다.
  - `value = 0`, 매우 큰 수, 음수, `meter:abc`, `meter`, `parsec:1.0` 경계값 및 오류 케이스를 정리했다.
  - Domain `95%+`, Boundary `85%+` 커버리지 목표와 `gcov`, `lcov`, `gcov UnitConverter.cpp` 측정 전략을 기록했다.
- `README.md`
  - `테스트 실행` 섹션 뒤에 `RED 단계 To-Do 리스트`를 추가했다.
  - Track A UI/Boundary 테스트 체크리스트와 Track B Domain/Logic 테스트 체크리스트를 추가했다.
  - 커버리지 목표와 결함 목록 연결 항목을 RED 단계 체크리스트로 정리했다.

백업 산출물:

- `prompt/03.테스트플랜및RED체크리스트_prompt.md`
  - 이번 작업 흐름의 사용자 요청, assistant 응답, 주요 도구 호출을 보존했다.
- `report/03.테스트플랜및RED체크리스트_report.md`
  - 작업 목적, 변경 파일, 요구사항 연결, 검증 결과, 미완료 항목, 다음 단계, GitHub 백업 결과를 기록한다.

## 3. 요구사항 연결

- `README.md`
  - 기본 단위 `meter`, `feet`, `yard`와 비율 `1 meter = 3.28084 feet`, `1 meter = 1.09361 yard`를 테스트 기준으로 사용했다.
  - 입력 형식 `<unit>:<amount>`와 출력 포맷 `<source_amount> <source_unit> = <converted_amount> <target_unit>`을 테스트 계획에 연결했다.
  - 테스트 없는 PR은 거부하고 RED/GREEN/REFACTOR 흐름으로 계약을 보호한다는 기여 방향에 맞춰 RED 단계 체크리스트를 추가했다.
- `docs/PRD.md`
  - Story 3 기본 단위 변환 요구사항과 README 기준 비율 회귀 보호 규칙을 반영했다.
  - 입력 파싱 실패, 음수, 미지원 단위, 원 입력 보존 등 Boundary/Domain 계약을 테스트 케이스로 정리했다.
  - Domain 라인 커버리지 `95%+`, Boundary 계약 테스트 목표를 테스트 계획에 반영했다.
- 사용자 요청
  - 코드 작성 없이 문서 중심으로 작업했다.
  - 기존 README 내용은 유지하고 새 `RED 단계 To-Do 리스트` 섹션만 삽입했다.
  - 관련 파일만 커밋 대상으로 삼고, 무관한 사용자 변경은 되돌리지 않았다.

## 4. 테스트 및 검증 결과

실행 또는 확인된 검증:

- `ReadLints`: `docs/test_plan.md` 진단 오류 없음.
- `ReadLints`: `README.md` 진단 오류 없음.
- `ReadLints`: 백업 전 `README.md`, `docs/test_plan.md` 재확인 결과 진단 오류 없음.
- `git status --short --branch`: 작업 브랜치가 `RED...origin/RED`이며 관련 변경 파일이 `README.md`, `docs/test_plan.md`임을 확인했다.
- `git diff -- README.md docs/test_plan.md`: README RED 체크리스트 추가 내용을 확인했다.
- `git log --oneline -5`: 최근 커밋 메시지 스타일을 확인했다.
- `ReadFile`: README, PRD, 테스트 계획서, 기존 보고서/프롬프트 양식을 확인했다.

미실행:

- CMake/Catch2 테스트는 실행하지 않았다.
- 사유: 이번 변경은 구현 코드나 테스트 코드 실행이 아니라 테스트 계획 및 README 체크리스트 문서화 작업이다.
- `gcov`/`lcov` 커버리지 측정은 실행하지 않았다.
- 사유: 커버리지 항목은 향후 RED/GREEN 테스트 구현 후 수행할 측정 전략으로 문서화되었다.

## 5. 미완료 항목

- Catch2 테스트 파일은 아직 작성되지 않았다.
- `meter -> feet` 변환 구현 또는 기존 구현 수정은 이번 작업 범위가 아니다.
- `defect_list.md`는 README 체크리스트에 연결 항목으로만 추가되었고, 실제 파일은 아직 생성되지 않았다.
- Domain/Boundary 커버리지 수치는 아직 측정되지 않았다.

## 6. 다음 단계

- RED 단계에서 `docs/test_plan.md`의 P0/P1 케이스를 Catch2 실패 테스트로 작성한다.
- GREEN 단계에서 `meter -> feet` happy path와 경계값 테스트를 통과시키는 최소 구현을 수행한다.
- 테스트 구현 후 `ctest`, `gcov UnitConverter.cpp`, `lcov` 리포트를 실행해 README/PRD의 커버리지 목표와 대조한다.
- 발견 결함이 있으면 `defect_list.md`를 생성해 결함과 회귀 확인 결과를 기록한다.

## 7. GitHub 백업 결과

- 백업 커밋: 커밋 후 갱신 예정
- 커밋 메시지: 커밋 후 갱신 예정
- 원격 업로드 결과: push 후 갱신 예정
- push 대상: `origin RED`
- 원격 저장소: 확인 후 갱신 예정

