# 12. 감쇠 단위 검증 백업 작업 보고서

생성일: 2026-05-21
워크스페이스: `E:\dev\unit_converter_13`
대상 브랜치: `FEATURE`

## 1. 작업 목적

신규 감쇠 단위 기능 GREEN 구현 완료 후 수행된 전체 테스트, bonus 태그 검증, 기존 회귀/Golden Master, 예외 동작, 커버리지 확인 결과를 문서화하고 백업한다.

이번 보고서는 구현 변경 보고서가 아니라 검증 이력 백업이다. 소스 구현 파일은 수정하지 않고, 접근 가능한 최신 요청 원문과 검증 결과를 `/prompt`, `/report`에 보존한다.

## 2. 변경 파일

백업 산출물:

- `prompt/12.감쇠단위검증백업_prompt.md`
  - 현재 접근 가능한 최신 보고서 작성 요청 원문과 검증 히스토리를 JSONL 원문으로 보존한다.
- `report/12.감쇠단위검증백업_report.md`
  - 전체 CTest 결과, bonus 검증, Golden Master, 예외 확인, 커버리지, 미완료 항목, 백업 결과를 기록한다.

소스 구현 변경:

- 없음.

커밋 제외:

- `build/`
- `build-coverage/`
- `*.gcov`
- 기타 CMake, Ninja, gcov 실행 산출물

위 파일들은 검증 중 생성 또는 갱신되는 산출물이므로 이번 백업 커밋에 포함하지 않는다.

## 3. 요구사항 연결

- `README.md`
  - 새 단위 추가 후 기존 `meter`, `feet`, `yard` 회귀 테스트를 다시 실행한다는 가이드와 연결된다.
  - `1 meter = 3.28084 feet`, `1 meter = 1.09361 yard` 기준 비율을 회귀 검증 대상으로 유지한다.
  - 동적 단위 등록 시 `ratio > 0` 계약과 연결된다.
- `docs/PRD.md`
  - §3.1 동적 단위 등록, 단위 레지스트리 요구사항과 연결된다.
  - §3.3 동적 등록 비율은 `0`보다 커야 한다는 제약과 연결된다.
  - §4.3 Domain/Boundary 커버리지 목표와 연결된다.
  - §7.2 새 기능 추가 시 기존 기본 단위 변환 테스트를 함께 실행한다는 회귀 보호 규칙과 연결된다.
- `docs/stories/c03-meter-feet-yard-conversion.md`
  - `meter`, `feet`, `yard` 기본 비율과 meter hub 변환 계약을 Golden Master 및 `test_converter` 회귀 검증으로 보호한다.

## 4. 테스트 및 검증 결과

이번 report-backup-agent 단계에서는 테스트를 재실행하지 않았다. 아래 결과는 직전 검증 히스토리로 전달된 실행 결과를 그대로 기록한다.

### 전체 CTest

- 명령: `cmake -S . -B build; cmake --build build; ctest --test-dir build -V`
- configure/build: 성공
- 빌드 출력 요약: `ninja: no work to do`
- CTest 결과: 3개 중 2개 PASS, 1개 FAIL
- PASS: `test_converter`
  - `All tests passed (21 assertions in 14 test cases)`
- PASS: `test_decaying_unit`
  - `All tests passed (14 assertions in 1 test case)`
- FAIL: `unit_converter_contract_tests`
  - 기존 `FAIL("RED")` placeholder 15건 때문에 실패
  - 이번 신규 기능 구현/검증 범위 밖의 기존 계약 테스트 placeholder 실패로 기록한다.

판단:

- 전체 CTest 상태는 FAIL이다.
- 단, 실패 원인은 신규 감쇠 단위 기능 또는 기존 회귀 테스트 결함이 아니라 `tests/unit_converter_contract_tests.cpp`의 의도된 RED placeholder다.

### Bonus / 신규 기능

- CTest 이름 필터 명령: `ctest --test-dir build -R bonus -V`
  - 결과: `No tests were found`
  - 원인: `-R`은 Catch2 태그가 아니라 CTest 테스트 이름을 기준으로 필터링한다.
- Catch2 태그 직접 실행: `.\build\test_decaying_unit.exe "[bonus]"`
  - 결과: PASS
  - 요약: 14 assertions in 1 test case

BT 항목별 상태:

- BT-01 `registerUnit("cubit", 0.4572)` 후 `cubit -> meter`: PASS
- BT-02 `meter -> cubit` 역변환: PASS
- BT-03 `cubit -> feet` 교차 변환: PASS
- BT-04 음수 비율 등록 시 `std::invalid_argument`: PASS
- BT-05 `convertAll("cubit", 1.0)` 전 단위 반환: PASS
- BT-06 기존 `meter -> feet` 결과 불변: PASS

### 기존 회귀 / Golden Master

- 명령: `ctest --test-dir build -R "test_converter|test_decaying_unit" -V`
- 결과: 2/2 PASS, 100%
- 의미:
  - `test_converter`가 기존 `meter`, `feet`, `yard` 회귀를 검증한다.
  - `test_decaying_unit`가 신규 감쇠 단위 bonus 기능을 검증한다.

직접 CLI Golden Master 출력:

```text
1 meter = 1 meter
1 meter = 3.28084 feet
1 meter = 1.09361 yard
```

```text
3.28084 feet = 1 meter
3.28084 feet = 3.28084 feet
3.28084 feet = 1.09361 yard
```

```text
1.09361 yard = 1 meter
1.09361 yard = 3.28084 feet
1.09361 yard = 1.09361 yard
```

판단: Golden Master PASS.

### 예외 확인

- 별도 임시 확인 프로그램으로 `registerUnit("cubit", -1.0)` 실행
- 결과: `std::invalid_argument` 출력
- 판단: 음수 비율 등록 거부 계약 PASS

### 커버리지

명령:

- `cmake -S . -B build-coverage -DCMAKE_BUILD_TYPE=Debug -DCMAKE_CXX_FLAGS="--coverage" -DCMAKE_EXE_LINKER_FLAGS="--coverage"`
- `cmake --build build-coverage`
- `ctest --test-dir build-coverage -R "test_converter|test_decaying_unit" --output-on-failure`

coverage 대상 테스트:

- `test_converter`: PASS
- `test_decaying_unit`: PASS

`gcov` 결과:

- Domain `Converter.cpp`: Lines executed 98.08% of 52
  - 기준 Domain >= 95% 충족
- Boundary `ConverterBoundary.cpp`: Lines executed 100.00% of 24
  - 기준 Boundary >= 85% 충족
- CLI `UnitConverter.cpp`: Lines executed 66.67% of 24
  - 참고 수치로 기록한다. 이번 Domain/Boundary 커버리지 통과 판단의 핵심 기준은 아니다.

특이사항:

- `gcov UnitConverter.cpp` 첫 시도는 경로 지정 문제로 notes/data file open 실패가 있었다.
- 이후 `.gcno` 경로 지정 및 coverage CLI 실행 후 `UnitConverter.cpp` 수치를 확보했다.

## 5. 테스트 목록 특이사항

현재 `tests/test_converter.cpp`에는 다음 테스트 ID만 존재한다.

- `TC-A-01~06`
- `TC-B-01~06`
- `TC-B-08`

요청에 언급된 `TC-A-07`, `TC-B-07` 이름의 테스트는 현재 코드베이스에 없다.

또한 `tests/unit_converter_contract_tests.cpp`의 15개 테스트는 모두 `FAIL("RED")` placeholder 상태다. 이 파일은 전체 CTest 실패의 직접 원인이며, 이번 신규 기능 검증 통과 여부와 분리해서 해석해야 한다.

## 6. 미완료 항목 및 다음 단계

- `tests/unit_converter_contract_tests.cpp`의 기존 RED placeholder 15건은 별도 RED/GREEN 단계에서 실제 계약 테스트로 전환하거나, 실행 범위를 명시적으로 분리해야 한다.
- CTest 전체를 항상 Green 기준으로 삼으려면 placeholder 테스트를 별도 라벨/브랜치/빌드 옵션으로 격리하는 정책이 필요하다.
- `build/`, `build-coverage/`, `*.gcov` 산출물은 커밋하지 않았지만 작업트리에 계속 남아 있으므로 필요 시 별도 정리 정책을 정한다.
- `TC-A-07`, `TC-B-07`이 요구사항상 필요하다면 다음 테스트 설계 단계에서 실제 테스트 ID로 추가해야 한다.

## 7. GitHub 백업 결과

- GREEN 구현 커밋: `eab1f4d`
- 구현 커밋 메시지: `feat(feature): add registerUnit with OCP registry`
- 이전 GREEN 보고서 커밋: `61a9644`, `335ab98`
- 이번 검증 백업 커밋: 커밋 후 기록 예정
- 원격 업로드 결과: push 후 기록 예정
- push 대상: `origin/FEATURE`
- 특이사항: 전체 CTest는 기존 `FAIL("RED")` placeholder 때문에 실패하지만, 신규 BT-01~BT-06, 기존 회귀 대상, Golden Master, Domain/Boundary 커버리지 기준은 통과했다.
